# Animations

This directory contains GIF animations known to work on CoolLEDUX / CoolLED1248 LED matrix displays.

Upload an animation using:

```bash
coolledux-upload animations/matrix.gif --auto --force
